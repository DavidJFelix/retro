defmodule Retro.CardContoller do
  use Retro.Web, :controller

  alias Retro.Card
  alias Ecto.UUID

  def index(conn, _params) do
  end


  def show(conn, %{"id" => id}) do
  end


  def create(conn, card_params) do
  end


  def update(conn, card_params) do
  end


  def delete(conn, %{"id" => id}) do
  end
end
